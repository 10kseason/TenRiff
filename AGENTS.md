# TenRiff Agent Notes

## Completed
- Large-library song indexing now has two explicit profiles: default `safe` mode keeps RAM low with streaming candidate enumeration, low-memory BMS parse, conservative worker/batch budgets, and periodic heap trimming, while optional `fast` mode raises the work budget for 32GB+ rescans.
- Real-world safe-profile benchmarking on `D:\Stellaverse (2025-12-14)` now completes `46,636` candidate scans at roughly `453MB` peak working set / `524MB` peak private memory, replacing the earlier multi-GB high-water behavior.
- The staged `0.8.0` Windows distribution payload and curated open-source source bundle now track the large-library indexing hardening and profile toggle, so `Baepoks\TenRiff-0.8.0*` and `opensource-Tenriff-source\TenRiff-0.8.0-source*` are the active delivery line.
- Difficulty calculation now ports the full local `10k-calc/new_calc.py` flow into C++ instead of a reduced approximation, including layout-specific matrix selection, Qwilight BMS EZ preset handling, and BMS lane-order alignment for scratch layouts such as `5+1 SP`, `7+1 SP`, and `14+2 DP`.
- Added direct Python-vs-C++ validation for the difficulty port: doctest coverage now calls the local `10k-calc` reference on representative fixtures, and a new `bms_10k_compare_smoke` runner compares random real-world BMS charts against the Python calculator to check rating/level drift on actual song packs.
- The staged `0.7.9` Windows distribution payload and curated open-source source bundle were refreshed again after the song-source cache safety hardening and latest verification pass, so `Baepoks\TenRiff-0.7.9*` and `opensource-Tenriff-source\TenRiff-0.7.9-source*` now match the latest runtime/source state.
- Initial CMake project scaffolding with doctest harness.
- Core BMS parser handling headers, dictionaries, and measure commands with strict/tolerant modes.
- Configurable channel-to-lane mapping defaulting to the combined 1P+2P 10-key layout.
- BMS chart normalization stage generating measure timings and sorted events with fractional #MEASURE support.
- Scheduling/timeline stage translating normalized events into absolute timing with BPM/STOP handling.
- Gameplay utility layer with SpeedManager (rate/HS calculations) and GaugeManager implementing the auto-shift gauge spec.
- Initial osu!mania loader capturing mode/key count, timing points, and columnized notes with validation helpers.
- Cross-platform launcher bootstrap scripts (.bat/.sh) that sanity-check assets/songs, seed default profiles, capture stdout/stderr to `logs/run.log`, and surface exit-code hints.
- Timeline conversion now yields **sample-aligned int64 timestamps** so the audio thread can consume deterministic events.
- Timing/Input scaffolding added: ClockSync regression estimator, POD InputEvent, and lock-free SPSC queue ready for platform hooks.
- Low-latency menu blueprint covering menu state machine, SongIndexerThread, cached song index, and latency-first settings/inputs.
- Core play loop skeleton: config/keymap loader, chart adapter, and audio-thread judgement pipeline.
- InputThread polling backend (1000/2000/4000/8000Hz) and RenderThread separation.
- Menu state machine with SongIndexerThread + cache (percent/ETA) and settings screens.
- Windows D3D11 menu UI (mockup-style Title + Song Select layouts) with key remap/NKRO test and result screen.
- Fixed Windows menu black-screen cases by hardening swap-chain target creation (alpha-mode fallback + logging) and removing remaining `path.string()` conversions that threw on non-ACP paths.
- Song index cache now stores BMS `PLAYLEVEL` for UI difficulty badges.
- Mode system (format/key/gauge/random) with FR/SR randomization and key-mode handling.
- Global config (`config/config.json`) + profile config integration.
- Replay capture + JSON export (replay + result) saved under `profiles/<name>/replays` and `profiles/<name>/results`.
- Non-Windows builds now exit with code 13 + a clear message that the GUI menu is Windows-only; renderer failures also propagate as exit code 13.
- Gameplay session now provides a real-time CLI HUD (progress/gauge/combo/judgement), `Esc` abort handling, and sample-domain synthesized guide/hit tones so play is no longer silent during charts.
- Gameplay launch no longer tears down the menu renderer (input/audio only pause), and MenuWindow now hard-fails/retries on missing D2D render targets to reduce black-screen stalls.
- Chart audio playback is now wired into gameplay: osu!mania `AudioFilename` and BMS channel `01`/`#WAVxx` cues are resolved and mixed in real time via WAV decode + sample-rate conversion.
- Removed per-frame gameplay HUD debug logging in `MenuWindow::draw`, eliminating a major render-thread stdout bottleneck that caused in-game slowdowns.
- In-game Hi-Speed control now uses `F3`/`F4` hotkeys (decrease/increase) with immediate gameplay reflection.
- Graphics target Hz now clamps to `1050`; menu rendering is effectively capped at `300`, while gameplay can use the configured value up to `1050`.
- Fixed a gameplay crash/freeze regression in HS hotkey handling by removing recursive `engine_mutex` locking in the audio-thread input path.
- Added in-game HS coarse hotkeys: `F5`/`F6` adjust Hi-Speed by `-10/+10` (with clamp).
- Default graphics FPS preset is now 1050 (runtime defaults + global/profile default config JSON updated).
- HUD note scroll mapping is now hit-line anchored (`delta=0` always at `y=0.82`) for both future/past notes, so HS changes no longer shift the judgement line alignment.
- Options now include a dedicated `Skins` screen with saved `Judge Line`, `Note Width`, and `Note Height` controls; gameplay rendering applies those settings live from `config.skin`.
- `Skins` now also supports per-mode lane-color editing for `4K..10K` plus `16K` with a live right-side preview; `config.skin.lane_colors` persists palette tokens, and gameplay note sprites/colors now resolve from those lane colors instead of the old hardcoded white/blue split.
- Song Select now supports four-way sorting (`LV ASC/DESC`, `TITLE A-Z/Z-A`), a `Browse` screen with live search plus key-count/difficulty filters, and a `Records` view that accumulates per-chart local play history, prioritizes clear status over raw score for best-record selection, and can reopen saved result/replay metadata from local JSON exports.
- Song Select now also supports `PageUp`/`PageDown` page navigation across songs, recent sources, and local records, with on-screen hints updated so keyboard-only users do not need the mouse wheel for fast browsing.
- Song Select navigation keys now repeat while held: holding `Up/Down` or `PageUp/PageDown` continuously scrolls Songs/Sources/Records (or the left nav cursor) after a short delay instead of moving only one step per key press.
- BMS parsing/indexing/runtime now honors explicit `#4K`, `#6K`, and `#8K` headers by compacting the chart’s used note channels into the declared lane count for menu display and gameplay, while osu!mania support remains lane-generic from `4K` through `10K`.
- Song Select now shows the selected chart’s background art on the right panel: osu!mania parses `[Events]` background images, BMS preview resolution prefers `#STAGEFILE/#BACKBMP` and then referenced `#BMPxx` BGA images, MenuApp resolves/caches the preview path per chart, and MenuWindow renders it through a single cached WIC bitmap with song metadata/stats underneath.
- Gameplay start now has a built-in `3 / 2 / 1` countdown: GameSession starts input first, drains countdown-time lane input without scoring, delays audio-thread start until the countdown ends, and MenuWindow renders a dedicated countdown overlay so notes do not begin descending until gameplay actually starts.
- osu!mania main-audio resolution is now more tolerant: ChartLoader still respects `AudioFilename` first, but if that field is missing/bad it now falls back to same-stem sibling audio (`.ogg/.wav/.wave/.mp3`) and then to the chart folder’s single audio file when unambiguous, so ogg-only/wav-only osu folders keep loading audio.
- In-game HUD note visibility now hides successfully judged notes immediately instead of letting them drift past the hit line. GameSession marks successful note heads as hidden, short notes vanish on hit, and active hold notes are redrawn as tail/body-only synthetic HUD notes anchored to the judgement line until release/end.
- MenuWindow now hides the Windows mouse cursor while gameplay HUD rendering is active and restores it on menu/quit transitions by toggling a local cursor-hidden state plus `WM_SETCURSOR`, avoiding the global `ShowCursor` counter.
- Graphics timing now treats VSync as monitor-driven instead of config-driven: MenuApp detects the active monitor refresh, fullscreen/window present refresh follows that detected Hz while VSync is on, and the render thread now targets `monitor_hz * 2` FPS under VSync (clamped to `1050`) instead of blindly reusing the saved `refresh_hz` cap.
- MenuWindow now also applies low-latency DXGI presentation tuning on swap-chain creation by setting maximum frame latency to `1` when the driver/runtime allows it, reducing extra queued-frame input lag even while VSync is enabled.
- The active packaged release line is now `0.8.0`: `build-dist\Release` is rebuilt as the packaging source of truth, `Baepoks\TenRiff-0.8.0` plus `Baepoks\TenRiff-0.8.0.zip` are the active binary package targets, and `opensource-Tenriff-source\TenRiff-0.8.0-source` is the active curated public source bundle line.
- A focused memory-leak audit is now in place: the doctest runner can enable Debug CRT leak dumping for local unit-test sessions, those tests currently exit without CRT heap leak reports, and `WasapiBackend` now balances `CoInitializeEx` with `CoUninitialize` so repeated audio-session init/shutdown no longer leaks COM apartment refs.
- Release metadata and packaging now target `0.8.0`: `build-dist\Release` is the source of truth for packaged binaries, `Baepoks\TenRiff-0.8.0` is the staged no-songs Windows distribution payload, and `opensource-Tenriff-source\TenRiff-0.8.0-source` is the curated source-only public bundle with an explicit scope manifest.
- Gameplay judge-line visuals are now better aligned with player feedback: the shared Y-position helper is used across static judge-line drawing and note placement, and the live lane-activity glow now appears on the configured judgement line instead of the field bottom.
- The staged Windows distribution package at `Baepoks\TenRiff-0.8.0` is refreshed from `build-dist\Release` and continues to ship only the no-songs runtime payload (`TenRiff.exe`, `config`, empty `songs`, `lib`, `LICENSE`).
- The curated public source bundle at `opensource-Tenriff-source\TenRiff-0.8.0-source` matches the latest source/docs/license state for the current release line.
- Menu renderer now enforces D3D11 hardware device creation (WARP/software fallback removed) and logs the active GPU adapter on init.
- Song indexing metadata parse now runs multi-core (hardware-concurrency worker fanout) with thread-safe progress/warning aggregation for faster large-library scans.
- BMS/OSU audio asset resolving now supports recursive fallback lookup under the chart folder (case-insensitive relative/file-name match) when direct path resolution misses.
- Gameplay chart audio prep now decodes unique WAV cues in parallel, and BMS playback now supports note keysound scheduling (`bms_keysound_policy=follow`) in addition to BGM channels.
- Chart audio decode now supports MP3/OGG on Windows via Media Foundation fallback (WAV remains native parser first), with stereo conversion + linear resampling into the engine mix rate.
- Fixed BMS timing regression: channel `03` is now parsed as hexadecimal BPM change (not BGA), and timeline tests now assert channel-03 BPM application so beat timing follows chart BPM changes.
- Song Select now loads per-chart best records from `profiles/<name>/results/*.json` and fills the right-side rank/score/judgement panel from persisted best results (not just last-play memory).
- Menu render loop now caches the latest snapshot and only deep-copies `MenuRenderData` when snapshot version changes, removing per-frame high-FPS copy churn/lock pressure.
- Gameplay HUD note rendering now uses sample-domain note positions plus render-time playhead extrapolation, removing the 16ms snapshot cadence judder that made motion look choppy despite higher render FPS.
- RenderThread pacing now avoids sub-millisecond sleeps and reschedules from post-render time, reducing high-FPS frame-time spikes when VSync is off or FPS limits are very high.
- Gameplay HUD refresh cadence is now 8ms and the audio callback no longer holds `engine_mutex` across the full mix path, reducing HUD/audio contention and making in-play updates feel smoother under load.
- Gameplay now auto-detects a chart-preferred sample rate from the primary music asset (osu!mania main audio, BMS largest cue) and reopens the gameplay WASAPI session at that rate when possible, reducing pitch/quality issues from forcing 48kHz across all songs.
- WASAPI shared-mode fallback now keeps a stereo-float gameplay stream (with Windows resampling when needed) instead of binding the engine directly to the device mix format, and audio defaults/presets now prefer 44.1kHz without silently overriding explicit sample-rate config.
- Shared-mode WASAPI event playback now uses the engine-chosen period (`Initialize(...,0,0,...)`) and fills the entire available buffer each wake-up, fixing severe stutter/slow-sounding playback when gameplay falls back from exclusive mode to shared mode.
- Chart asset resolution now falls back across matching audio stems, so missing `*.wav` references can automatically resolve to sibling `*.ogg`/`*.mp3` files instead of dropping keysounds/BGM.
- Gameplay synthetic guide/hit tones now stay disabled whenever real chart audio loaded successfully, and the final mix uses soft limiting instead of hard clipping so dense keysounded BMS charts no longer buzz or crackle as easily.
- Menu/runtime chart surface is now BMS-first by default: cached/scanned menu entries expose BMS-family charts normally, while an `OSU Charts` mode toggle can re-enable 10K `.osu` indexing/loading when desired.
- Menu UI now supports left-click hit-testing on title buttons, song-select navigation/cards, and row-based option screens; settings rows expose explicit `- / +` click zones, song cards use single-click select plus double-click launch, and keymap keeps keyboard capture while exposing click actions for save/reset/test/back.
- Audio settings now expose a saved master-volume slider, Mode settings now expose saved `Rate`/`Hi-Speed` rows, and gameplay applies the stored master gain to the final mix so menu changes persist across relaunches.
- AudioThread now passes the next write cursor sample (not `write cursor + padding`) into gameplay callbacks, removing an extra device-latency offset that made gameplay input, key sounds, and lane feedback feel sluggish.
- Gameplay input no longer requeues "future" events past the current buffer window; it clamps them to the active audio buffer instead, and Windows builds now prefer RawInput whenever `input.rawinput=true`, preventing delayed-key backlog that could make gameplay input feel stuck or progressively freeze.
- Gameplay HUD now keeps hold notes visible until their tail leaves the past window, and the D2D gameplay renderer draws an explicit tail cap so long notes no longer disappear halfway down or look open-ended.
- Gameplay note rendering now follows a 10K lane skin split: lanes 1/3/5/6/8/10 render white notes, while lanes 2/4/7/9 render blue notes, with the same color rule applied to hold heads, tails, and bodies.
- Song Select now supports menu-side sorting by difficulty or title from the left panel, and the left `SCORE` panel shows the selected chart's local best score inline when one exists.
- TenRiff now computes native osu!mania difficulty during song indexing across `4K..10K` using a generalized C++ port of the local `10k-calc` core model (`weighted NPS`, distance strain, jack strain, circus rating, revive level), caches both `rating` and `level`, and surfaces them in the song-select cards as `LV / CR`.
- osu!mania parsing now accepts the real-world `CircleSize` difficulty key as the key-count source (while keeping `KeyCount` as a legacy alias), so menu indexing and direct `.osu` loads work against normal beatmaps again.
- Song indexing now probes `.osu` headers before full parse, so non-mania or non-10K difficulties are skipped quietly instead of spamming parse warnings during library scans.
- Gameplay session now auto-reconciles stale profile `mode.format`/osu lane-mode mismatches against the detected chart, so old `bms`/`7k` profile values no longer block launching menu-selected osu!mania charts.
- Custom `deep-memory` skill is now tuned for long-running game-dev continuity, with better extraction of architecture decisions, regressions, commands, file refs, and performance metrics for multi-session work.
- BMS parser now ignores common `*-----` section separators and correctly treats header values containing `:` (for example `#SUBARTIST obj:u_e` or `#TITLE: ...`) as headers instead of malformed measure commands.
- Added `bms_realworld_smoke`, a non-interactive random real-pack BMS smoke runner that samples charts from `10Key-Revive-pack`, fails on any load warning/error, and prints the seed plus selected chart list for repro.
- BMS audio stem resolution now prefers `.ogg` over `.wav/.wave` when a chart references `*.wav`, but still falls back to the referenced WAV when no OGG sibling exists.
- BMS speed normalization no longer treats channel `08` BPM references as implicit base-36 BPM values; `#nnn08` now requires a declared `#BPMxx` entry, and fractional `#BPMxx` timing is covered by regression tests.
- BMS note keysounds are now attached per note and triggered on successful gameplay hits, while channel `01` BGM cues continue to autoplay; charts with only note keysounds still contribute to sample-rate detection/audio prep.
- BMS audio asset resolution now canonicalizes to absolute paths, and Media Foundation OGG/MP3 opens normalize relative paths before decode/probe so relative `Songs/...` runtime loads no longer fail at open time.
- Windows non-WAV chart audio decode now falls back to `ffmpeg.exe` when Media Foundation cannot open an OGG/MP3 cue, allowing real-world OGG-heavy BMS packs to keep loading audio on systems without MF codec support.
- Audio settings now split master/BGM/keysound gain, menu audio settings expose separate BGM and keysound sliders, and gameplay applies those gains independently so note keysounds can be boosted over BGM.
- Menu option sub-screens now track their caller, so `Esc`/Back from Audio/Graphics/Input/Mode/Keymap returns to the main menu when opened from Options, while Song Select still gets Mode Select back-navigation to Song Select.
- Runtime config now migrates stale BMS-first profiles on load, forcing `mode.format=bms`, `mode.key_mode=10k`, and `audio.bms_keysound_policy=follow`; audio presets no longer silently toggle keysound follow on/off behind the user's back.
- Song Select now responds to mouse-wheel input for chart navigation, with wheel movement focusing the song list and moving the selected chart directly.
- Result UI now has a dedicated D2D layout instead of the generic list view, showing rank/score/accuracy/gauge detail, a judgement mix panel, a gauge-history graph with shift markers, and session/export metadata.
- Menu startup/song-source switches are now cache-first: if the profile-local song-index cache exists it is loaded immediately for menu display and no background rescan starts until the user presses `F5` on the title screen; missing caches still trigger an index build.
- The Windows menu now accepts drag-and-drop folders (and BMS files via their parent folder), so directories outside the default `Songs` root can be dropped onto the window and indexed as the active song source.
- BMS keysound policy now supports `follow`, `autoplay`, and `ignore`: `autoplay` converts note keysounds into background audio cues so the chart plays a "no-keysound" version while key presses stay silent, and Audio Settings exposes the mode selector directly.
- BMS text decoding now normalizes file content to UTF-8 before parsing: UTF-8/UTF-8 BOM stays intact, UTF-16 BOM is handled, and Windows file loads fall back to CP932 (Shift-JIS) so Japanese/Kanji headers and `#WAVxx` asset names no longer arrive as mojibake.
- Song source browsing now persists recent folders in the active profile, and Song Select includes a `Sources` top-level view plus `Backspace` jump-to-sources behavior so previously loaded external folders can be reopened and their cached/indexed chart lists restored without re-dragging them.
- Input handling now stops at the foreground boundary: `InputThread` drops keyboard events whenever TenRiff is not the foreground process, and `MenuWindow` ignores click/wheel/file-drop UI events while the window is backgrounded.
- Graphics settings now expose a saved `Performance HUD` toggle, `RenderThread` keeps rolling frame-pacing history/low-FPS stats, and the menu renderer can draw a top-right frame-time graph with AVG ms/FPS, 0.1%/0.01% low FPS, and max FPS.
- Performance HUD overhead is now bounded: render performance stats use a 10-second rolling window plus fixed-size graph buffers, and MenuWindow caches graph geometry/text so the overlay stops injecting its own low-FPS spikes or keeping old hitch frames in the low-FPS numbers for too long.
- Gameplay HUD updates no longer republish the full menu snapshot every 8ms: live gameplay state now flows into the render cache through a dedicated gameplay-HUD revision path, and MenuWindow caches gameplay header/status text so in-game 0.1%/0.01% FPS is less affected by HUD copy/allocation churn.
- Performance HUD now reports gameplay cadence while a chart is active instead of the menu/render-thread cadence alone, and Song Select layout was rebalanced so the compact frame-pacing panel, right stats, footer hints, and left navigation stay inside the 1080p safe area without overlapping.
- Performance HUD graph rendering is now less jitter-prone: `PerformanceTracker` accepts zero-origin first samples, applies a graph-only median/weighted smoothing pass before publishing frame-time history, and keeps raw metric calculations unchanged so isolated visual-offset-induced spikes no longer dominate the overlay waveform.
- Render pacing is now smarter under `vsync=false`: `RenderThread` uses a hybrid high-resolution waitable-timer + adaptive oversleep correction + short spin-wait tail, and deadline advancement now stays aligned to the ideal cadence instead of drifting from post-render time after overruns, which reduces micro-stutter at very high caps like `1000fps`.
- Gameplay note rendering now avoids dynamic HUD note/lane vectors entirely by using fixed-size gameplay HUD buffers end-to-end, and the in-game note skin was simplified from rounded-rect + per-note opacity churn to cheaper rectangle primitives plus dedicated hold brushes to cut D2D draw/state overhead during dense charts.
- Gameplay rendering now caches the static playfield/lane-divider/hit-line/gauge-frame layer into a Direct2D command list and reuses it each frame, so the GPU only redraws dynamic notes/gauge fill instead of rebuilding the whole gameplay frame from immediate primitives every tick.
- Gameplay note heads/tails are now cached as GPU-side Direct2D bitmaps and drawn with `DrawBitmap` during play, shifting repeated note skin raster work away from per-frame CPU-side primitive setup and further reducing immediate-shape overhead.
- Graphics settings now expose a manual display-delay compensation value (`visual_offset_ms`) from `-500ms` to `+500ms`; positive values advance only the rendered gameplay visuals (not judgement/audio), and config load/save clamps the offset into the supported range.
- BMS long notes now build into gameplay hold notes: the default 10-key lane map includes `51`-`55` and `61`-`65`, `ChartLoader` pairs dedicated LN channels into `end_sample`, `#LNOBJ` markers also convert the preceding note on that lane into a hold, and only charts with `#LNMODE 2` mark those holds as charge notes with tail release timing judgement.
- BMS song indexing now computes native difficulty from the actual normalized/timed chart across supported compact key counts using the generalized local calc port (revive level + circus rating), caches those values in `song_index.json`, and menu difficulty sorting/display now uses those computed metrics instead of raw `PLAYLEVEL`.
- Gameplay input backlog now compresses stale lane events inside `GameSession` instead of replaying every delayed edge through the audio callback, and `GameplayEngine` has a non-scoring input-state sync path so stale hold releases still break holds without causing input-induced hitch spikes.
- Display-delay compensation no longer leaves gameplay note culling on the unshifted sample window: HUD note retention/lookahead now expand around `visual_offset_ms` with a small render slack, preventing display-offset charts from spawning notes late or dropping past notes early and looking "laggy" when visual offset is enabled.
- Project metadata is now named as release `0.7.9` in CMake, establishing the current build line as the `0.7.9` distribution baseline.
- Windows folder drag-and-drop now bypasses the menu foreground gate for `WM_DROPFILES`, so dropped external song folders/BMS files are accepted again even though background mouse/wheel UI input remains blocked.
- Keymap capture now allows any known virtual key except `Delete`: duplicate lane bindings no longer trigger a replacement-confirm flow, `Esc`/`Backspace` can be mapped normally, and unknown VKs round-trip through `VK_XX` tokens so far more keyboard keys can be saved/loaded.
- Default gauge tuning is now harsher again: `Hard` gains are reduced by `1 / 1.25` with `BD/PR` penalties multiplied by `1.25`, `Normal` uses `1 / 1.3` and `x1.3`, and `Easy` uses `1 / 1.4` and `x1.4`, with the same values pushed into development defaults and packaged config.
- Song index cache version mismatches are now treated as silent cache misses instead of user-facing warnings, and song rescans use a lower-memory path by capping parser workers to 4, referencing cached entries without copying them, and filling `SongIndex::entries` in place instead of holding a second full parsed-entry vector.
- `.osu` support is now optional instead of hard-disabled: `mode.enable_osu_charts` is persisted in config, Mode Settings exposes an `OSU Charts` toggle, song-index cache schema version 3 records whether `.osu` entries were included, and `ChartLoader` restores osu!mania parsing/main-audio loading whenever the toggle is enabled.
- Mode Settings now also exposes a `Chart Filter` row backed by `mode.format`: with OSU charts enabled the menu can show `BMS`, `OSU`, or `All`, while Song Select keeps the full indexed source in memory and rebuilds only the visible list when the filter changes.
- UTF-8/Korean filesystem paths are now handled more consistently on Windows: song-index cache load/save, song-root scans, song-indexer cache-directory creation, MenuApp path-key/display/root joins, and GameSession profile-root construction now convert UTF-8 strings through `std::filesystem::u8path(...)` instead of narrow `path(string)`/`ifstream(string)` calls.
- A Linux preview package is now staged under `Baepoks-Linuxs/TenRiff-0.5.0-linux-preview` with `launch_linux.sh`, current `config/config.json`, empty `songs/` and `logs/`, plus a README that explicitly states Linux GUI/audio/input backends are not implemented yet and no real Linux binary is bundled.
- Song Select indexing now draws a clear percent progress bar, and gameplay startup now emits staged chart-loading progress into the gameplay HUD so chart loads show both percent and stage text before notes appear.
- Graphics-setting saves no longer restart the render thread/window stack: render pacing config is updated live, MenuWindow applies pending window config without destroying the D3D/D2D device chain, and capture/Discord hooks should no longer get dropped just from saving graphics or visual-offset changes.
- UTF-8/UI path handling is now lossy-safe in the menu/render path: `MenuWindow` text conversion no longer assumes perfect UTF-8, `MenuApp`/`SongIndex` path construction uses a fallback decoder instead of raw `u8path`, and malformed legacy/cache bytes should no longer crash Song Select when scrolling onto CJK-heavy or mojibake entries.
- Graphics settings now expose explicit `Resolution` and `Refresh Hz` controls: resolution cycles `720p / 1080p / QHD / Monitor Native`, refresh is clamped to `60..1050 Hz`, menu rendering is capped at `300 Hz`, gameplay uses the configured value up to `1050 Hz`, and MenuWindow now honors the chosen size instead of always forcing full-monitor dimensions on startup.
- Default per-mode keymaps now ship as `4K: D F L ;`, `5K: D F K L ;`, `7K: W E R M I O P`, `8K: W E R V M I O P`, `10K: Q W E R V M I O P [`.
- Gameplay chart loading now accepts `Esc` cancel during synchronous initialization: MenuApp polls a fresh `VK_ESCAPE` press while `GameSession::initialize(...)` is running, GameSession checks that cancel request between major load stages and during chart-audio decode prep, and user-driven cancels return to Song Select without logging a hard initialization failure.
- osu!mania support now covers `4K..10K` end-to-end: mode parsing/apply logic accepts `4k..10k`, song indexing no longer drops 4K-9K `.osu` entries, the loader keeps non-10K lane counts, and native difficulty calculation now runs across those key counts instead of stopping at 10K-only charts.
- Keymaps are now stored per key mode (`4k`..`10k`, `16k`) inside `keymap.json`: legacy top-level `bindings` are migrated into the 10K map, MenuApp lets the user switch the keymap editor/test screen between key modes, and GameSession picks the active binding set from the final chart lane count so different osu key modes can use different inputs at runtime.
- Song Select crash hardening now sanitizes menu-facing UTF-8/control-heavy metadata, syncs Song Select selection/view invariants before input and snapshot publish, makes best-result path-key lookup no-throw, and renders placeholder text instead of crashing when a card/source string is malformed.
- Large-library song indexing is now stage-aware and less commit-heavy: SongIndexerThread reports `SCANNING FILES / BUILDING METADATA / WRITING CACHE`, metadata work runs in RAM-aware batches that leave a physical-memory reserve, processed candidates are released batch-by-batch, and song-index cache saving now streams JSON entries directly to disk instead of materializing one giant JSON tree/string in memory.
- Documentation handoff is now stronger: `docs/README.md` maps the repo docs, `docs/current-state.md` is the primary current-behavior handoff doc, and `docs/config.md` was refreshed to match the actual live config/profile/keymap structure.
- The root `README.md` is now a fuller Korean onboarding document that explains the product, current capabilities, build/run flow, package layout, and how to continue into `docs/README.md` for deeper reference.
- Public-facing README/docs entrypoints now avoid internal Codex-specific references such as `.codex-memory`; onboarding stays focused on product docs and normal build/run usage.
- Loading-memory hardening is now in place across menu and gameplay: `song_index.json` cache load parses entries as a stream instead of materializing a full JSON DOM, `MenuApp` keeps the visible song list as index views over the owned song table, `GameplayChart` interns audio asset paths into a shared asset table, and `GameSession` now uses staged audio preload/background decode/prefetch/eviction with one-shot process-memory diagnostics around cache load, visible-list rebuild, chart parse, startup preload, and steady-state warmup.
- Gameplay feel/UI polish is now extended: future notes ease in through a dedicated top entry band, gameplay ends immediately after the final judged note/hold resolves, Skins adds saved `LN Body Width`, Song Select has a draggable mouse scrollbar, and Graphics adds a movable `windowed` display mode.
- BMS SP compact layouts are now supported end-to-end: parser/index/runtime auto-detect explicit or headerless `5+1 SP` / `7+1 SP`, song-index cache schema is now version `7` with optional `layout_label`, and menu/detail surfaces show those layout labels while runtime still uses 6K/8K bindings.
- BMS parser/index/runtime now also support `.pms` `9K` and `#PLAYER 3` `14+2 DP`: `.pms` extension fallback infers `PMS 9K`, DPA charts map into full 16-lane runtime/keymap/skin paths, and song-index cache schema is now version `7` so old caches silently rescan into the new key counts/layout labels.
- Headerless/common-layout BMS key inference is now broader: when explicit key headers are absent, standard SP/DP channel templates infer lane counts/layout labels before falling back to compact used-lane counts, so menu/detail no longer collapses most BMS charts to `10K BMS`.
- Headerless `7+1 SP` parsing/loading is now covered by dedicated doctest regressions alongside the existing explicit/headerless SP cases, so parser and `ChartLoader` both assert the 8-lane compact path.
- Gauge tuning now uses one-way threshold downshifts (`Hard -> Normal` at `66`, `Normal -> Easy` at `33`) with no refill on shift, no upshift path, and harsher default delta tables.
- Gauge defaults are now much stingier on recovery and harsher on mistakes: all `PG/GR/GD` gains were cut to `44.5%` of the prior table, `BD` penalties increased by `20%`, `PR` penalties increased by `10%`, and runtime migration upgrades legacy default gauge tables in existing profiles without overwriting custom gauge tuning.
- Gauge recovery was reduced again after that retune: the already-lowered `PG/GR/GD` gains are now only `25.75%` of the previous recovery table, while `BD/PR` penalties stay at the harsher values; runtime migration now upgrades both the original and the immediate prior default gauge tables to the latest defaults without touching custom user tuning.
- Gauge penalties were then raised again: current default `BD/PR` losses are now `1.65x` harsher than the immediate prior table across Hard/Normal/Easy, and runtime migration upgrades the original, intermediate, and immediate-prior default gauge tables to those latest penalty values while leaving custom user tuning intact.
- `BAD` was then made even harsher on top of that penalty pass: current default `BD` losses subtract an additional flat `5.0 / 2.5 / 1.75` on Hard / Normal / Easy respectively, while `PR` stays on the prior harsher table; runtime migration upgrades the immediate-prior defaults to those latest `BD` values without touching custom user tuning.
- Gauge deltas are now absolute at runtime instead of being scaled by hold-note half weights: `GameplayEngine` feeds full per-judgement values into `GaugeManager`, so gauge recovery/loss no longer changes with hold head/tail weighting or similar chart-structure scaling, and regression coverage now asserts full-value hold gauge gain.
- Chart-audio playback now honors gameplay `Rate` at the mixer stage instead of only scaling cue timings, so BMS main BGM and keysounds no longer stack/tear when rate is slower or faster; playback duration and linear-interpolated mix behavior are covered by `test_chart_audio_playback.cpp`.
- Song Select now exposes a quick `KEY` filter directly on the left navigation, and the dedicated `Browse` screen keeps the same key-filter state so key-count filtering no longer requires drilling through multiple menus.
- Skins now supports `rect`/`circle` note shapes, note-border on/off, and saved `Combo Y` positioning with live preview parity between the Skins screen and gameplay HUD.
- Skin sizing is now key-mode aware: `note_width_scales` / `note_height_scales` persist per mode (`4K..10K`, `16K`), gameplay and skin preview shrink the centered playfield when note width is below 100%, and `circle` notes bypass bitmap sprites so they render as true circles instead of stretched ovals.
- Default judgement timing now widens only `BAD` to `200ms`; config defaults, save/load, legacy `80ms` profile migration, and regression tests were updated to match.
- Hold-tail judgement is now chart-accurate by source: osu!mania holds keep release timing, standard BMS long notes auto-clear when held to the tail, and BMS `#LNMODE 2` charge notes enable tail release timing per chart.
- The `0.7.6` Windows distribution payload was refreshed again from `build-dist\Release` after the gameplay frame-pacing/UI readability pass; `Baepoks\TenRiff-0.7.6`, `Baepoks\TenRiff-0.7.6.zip`, and `Baepoks\TenRiff-0.7.6-latest.zip` now contain the current executable and runtime libs.
- The `0.7.6` Windows distribution payload was refreshed again from `build-dist\Release` after the harsher gauge retune and broader BMS key-count inference fix, so `Baepoks\TenRiff-0.7.6` and both `0.7.6` zip artifacts now match the latest `build-dist` executable/config/libs.
- The `0.7.6` Windows distribution payload was refreshed again from `build-dist\Release` after the chart-rate audio playback fix, so `Baepoks\TenRiff-0.7.6`, `Baepoks\TenRiff-0.7.6.zip`, and `Baepoks\TenRiff-0.7.6-latest.zip` now include the updated BMS rate-audio build.
- The `0.7.6` Windows distribution payload and curated open-source source bundle were refreshed again after the render-pacing/high-FPS updates, so `Baepoks\TenRiff-0.7.6*` and `opensource-Tenriff-source\TenRiff-0.7.6-source` now match the latest `build-dist` binary plus current repo source/docs/config state.
- The `0.7.6` Windows distribution payload was refreshed again after the latest gauge-recovery retune, so `Baepoks\TenRiff-0.7.6`, `Baepoks\TenRiff-0.7.6.zip`, and `Baepoks\TenRiff-0.7.6-latest.zip` now ship the updated `build-dist` executable/config/libs.
- The `0.7.6` Windows distribution payload was refreshed again after the latest `BAD` gauge retune, so `Baepoks\TenRiff-0.7.6`, `Baepoks\TenRiff-0.7.6.zip`, and `Baepoks\TenRiff-0.7.6-latest.zip` now match the newest `build-dist` executable/config/libs.
- The `0.7.6` Windows distribution payload and curated open-source source bundle were refreshed again after the per-key-mode skin sizing/playfield-width update, so `Baepoks\TenRiff-0.7.6*` and `opensource-Tenriff-source\TenRiff-0.7.6-source*` now match the latest `build-dist` binary and current repo source/docs/config state.
- Release naming has now advanced to `0.7.9`: project metadata/docs/package scope were bumped, and new `Baepoks\TenRiff-0.7.9*` plus `opensource-Tenriff-source\TenRiff-0.7.9-source*` artifacts are the current delivery line while the old `0.7.8` artifacts remain as historical snapshots.

## TODO / Next Steps
- Flesh out audio/render subsystems per the development manual (gameplay renderer polish, non-WAV codecs, full BMS keysound follow mode).
- Track staged implementation roadmap in `docs/roadmap.md` (Audio master clock → full song loop → key remap/8K-10K → launcher).
- Deepen BMS menu metadata beyond raw `PLAYLEVEL` if richer difficulty sorting/rating becomes necessary.
- Extend launcher to inspect real TenRiff binary metadata (version, build info), surface SDL2/VC++ runtime guidance, and collect/tail logs per exit code.
- Validate and tune cross-key (`4K..10K`) difficulty calibration against larger real-world BMS/osu libraries if menu rating parity becomes a product requirement.

## Working Agreements
- When you complete or significantly advance a task, update the Completed/TODO lists so the next agent has accurate context.
- Keep instructions here concise and actionable; prefer bullet lists over long prose.
